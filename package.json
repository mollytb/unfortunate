{
  "name": "unfortunate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "async": "^2.6.1",
    "mongoose": "^5.1.6",
    "parallel": "^1.2.0"
  },
  "scripts": {
    "start": "concurrently 'npm run server' 'cd client && npm start'",
    "server": "node server",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^3.6.0"
  }
}
